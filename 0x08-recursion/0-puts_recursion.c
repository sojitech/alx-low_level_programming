#include "main.h"

/**
* _puts_reursion - prints's a string followed by  new line
* @s: string to be printed
* 
* Return: void
*/
void _puts_recurse(char *s)

{	if (*s == '0')
	{
		_putchar('\n')
	}
	esle
	}
		_putchar(s[0]);
		_puts_recursion(s +1)
	}
}
